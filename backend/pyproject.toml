[project]
name = "expense-tracker-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "psycopg2-binary",
    "python-dotenv",
    "python-jose[cryptography]",
    "passlib[argon2]",
    "python-multipart",
]
